app:
  send_mode: "draft"   # draft 또는 send

paths:
  input_dir: "./input_pdfs"
  daily_output_dir: "./daily_json"
  weekly_output_dir: "./weekly_reports"

llm:
  base_url: "YOUR_BASE_URL"
  api_key: "YOUR_API_KEY"
  model_text: "YOUR_MODEL_NAME"
  model_vision: "YOUR_MODEL_NAME"

weekly:
  send_day: "FRI"
  send_hour: 13
  send_minute: 0

outlook:
  subject_prefix: "[주간 글로벌 정세 동향]"
  to:
    - "example1@company.com"
    - "example2@company.com"
  cc: []